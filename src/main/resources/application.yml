spring:
  datasource:
    url: jdbc:h2:mem:localdb
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2
server:
  port: 8080
  servlet:
    context-path: /demo/api
